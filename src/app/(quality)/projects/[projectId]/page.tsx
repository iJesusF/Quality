import { notFound } from "next/navigation";
import { requireUser } from "@/lib/auth";
export const dynamic = "force-dynamic";
export default async function ProjectPage({ params }: { params: Promise<{ projectId: string }> }) { const { projectId } = await params; const { supabase } = await requireUser(); const { data, error } = await supabase.from("projects").select("id,name,code,client,location,status,description,start_date,end_date").eq("id", projectId).maybeSingle(); if(error) throw new Error(error.message); if(!data) notFound(); return <div className="module-page"><section className="page-heading"><div><div className="eyebrow"><span>{data.code}</span></div><h1>{data.name}</h1><p>{data.client || "Cliente no configurado"}</p></div></section><section className="project-detail"><dl><div><dt>Estado</dt><dd>{data.status}</dd></div><div><dt>Ubicación</dt><dd>{data.location || "Pendiente"}</dd></div><div><dt>Inicio</dt><dd>{data.start_date || "Pendiente"}</dd></div><div><dt>Fin</dt><dd>{data.end_date || "Pendiente"}</dd></div></dl><p>{data.description || "Agrega la descripción y configuración de calidad del proyecto."}</p></section></div>; }
