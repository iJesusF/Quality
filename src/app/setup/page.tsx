import Link from "next/link";
export default function SetupPage() {
  return <main className="auth-page"><section className="auth-card"><div className="auth-brand"><span>V</span><div><strong>VORTECH</strong><small>QUALITY</small></div></div><h1>Conecta Supabase</h1><p>La aplicación no utiliza datos simulados. Configura estas variables en Vercel para activar autenticación y datos reales:</p><pre>NEXT_PUBLIC_SUPABASE_URL{`\n`}NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY</pre><ol><li>En una base nueva, ejecuta las migraciones una vez y en orden. Si ya existe <code>user_profiles</code>, no repitas la migración 001: aplica solamente las incrementales pendientes.</li><li>Agrega ambas variables en Vercel → Settings → Environment Variables.</li><li>Redeploy y abre la aplicación.</li></ol><Link className="primary-button" href="/login">Comprobar configuración</Link></section></main>;
}
